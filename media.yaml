- name: Template compose file
  template:
    dest: "{{ data_dir }}/compose/docker-compose.yaml"
    src: docker-compose.yaml
- name: Deploy containers
  docker_compose:
    project_src: "{{ data_dir }}/compose"
    remove_orphans: true
    state: present